# 討論與決策紀錄

> DeSci Brokers｜移工保險基礎設施 Migrant Insurance Infrastructure
> 語復與 AI 助理的工作紀錄整理，2026-08-29。
> 給隊友看的重點是：**哪些事情做了決定、為什麼這樣決定、以及哪些是刻意沒做的。**

---

## 目錄

1. [開發環境：為什麼不換工具](#1-開發環境為什麼不換工具)
2. [Docker 與 vLEI sandbox](#2-docker-與-vlei-sandbox)
3. [交件規範：Demo Day 的形式變了](#3-交件規範demo-day-的形式變了)
4. [主軸變更：從健檢綠燈改為理賠](#4-主軸變更從健檢綠燈改為理賠)
5. [vLEI 到底驗證了誰](#5-vlei-到底驗證了誰)
6. [Agent 框架：引擎與情境分離](#6-agent-框架引擎與情境分離)
7. [假資料標注政策](#7-假資料標注政策)
8. [金鑰處理](#8-金鑰處理)
9. [實際跑之後抓到的四個 bug](#9-實際跑之後抓到的四個-bug)
10. [目前狀態與未完成項目](#10-目前狀態與未完成項目)

---

## 1. 開發環境：為什麼不換工具

**問**：開發黑客松是否要用 CLI 模式？

**決定：不換。** 環境裡沒有 `claude` CLI、沒有 node，要換得先裝一輪，那是 1–2 小時，
而且全部發生在還沒寫任何一行 demo code 之前。黑客松前一天換開發環境不划算。

順手掃出來的工具鏈狀況，這個對開發影響比較大：

| 工具 | 狀態 | 影響 |
|---|---|---|
| `uv` | ✅ | Python 環境用這個 |
| `python3` | ⚠️ 3.9.6 系統內建 | 太舊，要用 `uv venv --python 3.12` |
| `go` | ❌ 沒裝 | **這一輪砍掉 Go，只用 Python** |
| `docker` | ❌ 沒裝 | 見下一節 |

原本 vault 寫技術棧是「Golang / Python」，這輪收斂成 Python——
Go 沒裝，而且 RAG 與 LLM 的生態在 Python 上快得多。

---

## 2. Docker 與 vLEI sandbox

**問**：幫我安裝 docker。

**已完成**。Docker Engine 29.7.2 + Compose v5.4.0，官方 `desktop.docker.com` 下載，
簽章驗證為 Docker Inc (9BNSXJN65R)、Apple 公證。

**但最後沒有用到 Docker。** vlei-sandbox 有 mock 模式，純 Python、零外部相依，
足以完成憑證簽發、撤銷與驗證。Docker 只有 `real` 模式（KERIA）才需要，
那是協定層的整合驗證，不是這兩天的範圍。

> **給隊友**：不用裝 Docker 也能跑完整流程。

---

## 3. 交件規範：Demo Day 的形式變了

**交件內容沒有變少**——repo、README、Demo 連結、簡報全都要交，8/30 23:59 截止。

**變的是上台那 5 分鐘的形式**：

- Demo Day **不做現場實機操作**，一律播放預錄影片
- 上台只播簡報一份，檔名 `隊伍名稱_DEMO`
- **主辦電腦播放，不接受自備筆電**
- 5 分鐘**含影片播放時間**，超時不計分

**實際影響只有一個，但很大：評審看不到系統跑起來，只看得到錄影。**
能不能跑、跟拍不拍得出來，是兩件事——後者才是計分的那件。

---

## 4. 主軸變更：從健檢綠燈改為理賠

8/28 的 Concept deck 有兩句明文改了方向：

> 「RBA 是企業端的**加分價值，而不是產品主軸**」
> 「把 vLEI 設計成**可插拔的** organizational trust layer，**而不是 MVP 前置條件**」

| | 先前（8/25 + build/） | 現在（0829 deck） |
|---|---|---|
| 核心 | 職安健檢綠燈憑證 | **移工職災理賠（Migrant Insurance Infrastructure）** |
| RBA 定位 | 商業價值主軸 | 企業端副產品 |
| 角色 | 移工／醫院／稽核員 | 移工／仲介／雇主／保險／稽核，五方同案 |
| AI 份量 | 較輕 | 多語事故理解排第一能力 |

**決定**：主軸換成理賠，**健檢綠燈降級為理賠包裡的一項證據**
（在 `build_protection_record()` 的 `evidence_credentials` 還看得到它）。
`health_pass_scenario/` 的遮罩計畫與合成資料全部保留，沒有浪費。

六步流程 `Verify → Understand → Match → Claim → Track → Record` 直接對應 deck。

---

## 5. vLEI 到底驗證了誰

這是最容易被評審追問的一題，也是我們問最久的一題。

### 兩層都要驗

| 層 | 憑證 | 這個案子裡是誰 |
|---|---|---|
| **法人層** | LE 憑證（公司有 LEI） | 雇主晶宏電子、仲介宏泰、醫院、保險公司 |
| **角色層** | ECR 憑證（人代表公司） | 仲介承辦人陳美玲、醫院主檢醫師 |

順序是：先確認公司是真的（LE），再確認**這個人被那家公司授權**（ECR）。

### 阮氏梅沒有憑證

**LEI 是法人識別碼，自然人不在適用範圍。** 移工那一層是同意收據，不是 vLEI。
這是刻意分層，要主動講——不講會被當成漏洞。

### 「驗證的人」是誰

- **誰去驗** = 需要相信這份資料的人。理賠情境是保險／理賠審核方。
  驗證離線跑，拿到呈現包就能自己驗，**不必打電話回醫院**。
- **誰被驗** = 組織（LE）+ 代表該組織行動的自然人（ECR）

### 為什麼醫師和承辦人用 ECR 不用 OOR

**OOR** 主張公司登記文件上查得到的職務（董事、執行長），**只有 QVI 能發**。
**ECR** 主張法人自己定義的功能性角色，**法人可以直接發**。

「理賠承辦人」「職業醫學科主檢醫師」都不在公司登記文件上，所以是 ECR。
這也讓落地路徑短很多：不必為每個承辦人走 QVI 流程。

### 驗證器實際跑的七步

1. 重算每個 SAID，確認內容未被竄改
2. 比對 schema SAID 是否為該憑證型別
3. 用**簽發當時**的金鑰驗簽章（所以換金鑰不會廢掉舊憑證）
4. 驗 LEI 檢查碼（ISO 17442-1，mod 97）
5. 查 TEL 註冊狀態（有無撤銷）
6. 沿 edge 遞迴，強制 I2I 身分約束
7. **確認信任根就是預期的 GLEIF**

第 7 步最常被跳過也最關鍵：一條完美驗證回「別人的」信任根的鏈，什麼都沒證明。

### 它證明什麼、不證明什麼

> **vLEI 證明的是「出處與授權」，不是「內容為真」，更不是「行為合規」。**

| 證明成立 | 刻意不主張 |
|---|---|
| 這家公司是登記在案的那一家 | 健檢或診斷內容正確 |
| 簽的人確實被授權以該身分簽 | 專業判斷無誤 |
| 簽發後一個字沒被改 | 這家工廠沒有強迫勞動 |
| 此刻仍然有效（沒被撤銷） | 對方拿到後不會另存 |

**右邊那欄不是弱點，是讀懂規格的證據。** GLEIF 在每張憑證的規則區塊裡自己就寫著：
有效憑證不主張該實體聲譽良好或合規。**先講的人拿分，被問出來的人扣分。**

### 名詞

| 名詞 | 白話 |
|---|---|
| **SAID** | 文件的指紋，而且指紋寫在文件自己裡面。改一個字就對不上 |
| **TEL** | 發行與撤銷的唯附加紀錄。撤銷就是往這裡寫一筆 |
| **KEL** | 金鑰事件日誌。讓驗證用「簽發當時」那把金鑰 |
| **ACDC** | 憑證格式，特點是憑證可以掛在另一張憑證上 |
| **I2I edge** | 約束「誰有資格站在發行者的位置」 |
| **worker protection record** | **0829 deck 自造的詞，非業界標準術語。**「事故→理賠→回應→改善→證據」串成一份可驗證紀錄 |

---

## 6. Agent 框架：引擎與情境分離

**決定**：`trustagent/` 是引擎，**不含任何情境知識**；`scenarios/` 是情境，換皮只換這裡。

```
trustagent/          引擎 — 沒有一行知道「理賠」或「健檢」
  models.py          Principal / Grant / Tool / Decision
  gate.py            政策閘：撤銷 > 到期 > 範圍 > 風險
  audit.py           雜湊鏈紀錄 + 竄改偵測
  verifier.py        出處驗證介面（Mock / vLEI sandbox）
  llm.py             OpenRouter 客戶端（stdlib，零相依）
  agent.py           執行迴圈
  console.py         終端輸出

scenarios/           情境 — 換皮只換這裡
  migrant_claim.py   移工職災理賠（主線）
  health_pass.py     職安健檢綠燈（現為證據之一）
  medical_claim.py   個人醫療理賠（證明可複用）
```

`run.py tour` 跑三個情境驗證這件事，引擎一行不動。

### 四道關卡的順序本身是設計

```
① 代理關係（你是誰的人）→ ② 撤銷/到期 → ③ 授權範圍 → ④ 不可逆動作攔截
```

代理關係驗證**刻意排在政策閘之前**——連「你是誰的人」都沒確認，談授權範圍沒有意義。

### 稽核鏈自己寫，不依賴 apl-sidecar

用 stdlib 的 sha256 做雜湊鏈，`verify` 和 `tamper` 零外部相依。
好處是少一個現場會壞的東西——原本的計畫依賴 `apl-sidecar` 的 CLI 旗標，那還沒驗證過。

### 最強的一幕：Corppass

三國比較研究裡最有價值的一條，來自新加坡：
**驗的不是「這家公司是真的」，而是「這位承辦人被公司授權處理本案」。**

```
陳美玲（在職）  ✓ 可協助整理文件
                ✕ 但碰不到雇主責任紀錄（scope 較窄）
林志豪（離職）  ✕ ROLE_NOT_VERIFIED：憑證已被撤銷，整條鏈失效
```

同一家仲介、同一個請求，差別只在那個人的角色憑證還有沒有效。
離職員工帳號沒收回是每個做過企業系統的人都被咬過的問題，
而且它同時演完「身份」「授權」「撤銷」三個評分要點。

### AI 與密碼學的分工

> **AI 負責理解與代理，密碼學負責證明，人類負責不可逆的那一下。**

| 環節 | 誰做 |
|---|---|
| 母語事故理解 | AI（實際呼叫模型） |
| 出處與授權證明 | 密碼學（**AI 完全不參與**） |
| 決定遮罩哪些欄位 | **宣告式規則——刻意不讓 AI 決定，規則不會幻覺** |
| 正式送件 | 人類確認 |

### 轉人工用欄位完整性，不用信心分數

原本寫的是「信心值低於門檻就轉人工」。**這個設計在 Q&A 站不住**——
評審只要問「為什麼是 0.9 不是 0.85」就沒有答案。

改成：**必填欄位（事故日期、受傷部位、事故機轉、是否因執行職務）
只要有任一項抽不到，或模型自己標記為推測而非讀取，就轉人工。**

這個規則說得出口、可以當場示範，也對應主辦方要求的人工確認機制。

---

## 7. 假資料標注政策

**規則第十四節「要求」敏感情境使用合成資料，第十七節禁止的是「偽造 Demo 結果」。
界線在有沒有講清楚。**

評審扣的是「宣稱與實作不符」的分，不是「這是原型」的分。

### 標注做三層

1. **程式碼裡** — 每個檔案開頭有聲明區塊，每個函式的 docstring 寫明
   「現況是什麼、真實版本該怎麼做」，每個假值旁邊掛 `[假資料]`
2. **畫面上** — 開場一個黃框總聲明，每一步印出該格的實際來源
3. **`FIXTURES.md`** — 完整清單，分成「完全是假的／格式真內容假／格式也不對／已移除」

**開場聲明依實際狀態產生，不是寫死的。** 有金鑰時印「真的呼叫模型（模型名）」，
沒金鑰時印「未設定金鑰，本次使用固定值」。所以錄影本身就帶著誠實聲明。

### 標注錯了跟沒標一樣糟

接上真東西之後，記得把對應的標注一起改掉。這個在這次過程中發生了兩次：

- 接上 LLM 後，模組聲明還寫著「沒有任何語言模型被呼叫」
- 接上真 vLEI 後，畫面還寫著「撤銷判定來自 MockVerifier 的查表」

### 已移除的東西

| 項目 | 為什麼 |
|---|---|
| **具體法條條號** | 那是編出來、沒查證的。簡報出現未查證的法條，被抓到直接失去可信度 |
| **信心值 0.91** | 沒有計算依據的數字，會引來「為什麼是 0.9」的追問 |

### LEI 檢查碼必須是有效的

原本用的三個 LEI **全部沒通過 ISO 17442 的 mod 97 檢查碼**。
這不只是資料不真實——**`vlei-sandbox` 會拒絕檢查碼不合的 LEI**，
用隨手編的字串發憑證會直接被擋，白白耗掉除錯時間。

已全部重算，sandbox 自己的 `lei check` 工具確認 VALID：

```
8945001XYZTAIWAN0024  醫院 仁德
8945002HONGTAI00TW15  仲介 宏泰
8945003FUBONLIFE0T38  保險 富邦
8945004JINGHONG0TW26  雇主 晶宏
```

> ⚠️ **簡報裡如果還有 `8945002HONGTAI00TW01`（結尾 01），那是舊的無效值，要改成 `...TW15`。**

### 自訂名詞要標明

**本專案自行命名**、不是業界標準的：

- scope 名稱 `claim_prep` / `employer_record` / `full_medical` / `rba_audit` / `worker_self`
- 決策碼 `ROLE_NOT_VERIFIED`、`OUT_OF_SCOPE`、`NEEDS_HUMAN_CONFIRMATION`

沿用外部來源、可以照講的：LEI / vLEI / ECR / OOR / QVI / SAID / ACDC / KERI / TEL / CESR（GLEIF、ISO 17442）、
六步流程（0829 deck）、organization-to-person authorization（新加坡 Corppass）。

---

## 8. 金鑰處理

**用 OpenRouter，模型 `anthropic/claude-sonnet-5`。**

技術選擇：**用 stdlib `urllib`，沒裝 SDK。** OpenRouter 是 OpenAI 相容介面，
一個 JSON POST 就夠。這樣保住「零外部相依」——不用跟 pip 和 venv 搏鬥。

### 規則

- **金鑰只從 `.env` 或環境變數讀取，程式碼裡不出現任何金鑰**
- `.env` 已被 `.gitignore` 擋住（已用 `git check-ignore` 實測）
- **不要把金鑰貼進聊天視窗、LINE 群組、簡報或任何會截圖的地方**
- 各自去 OpenRouter 開一把，不要共用

### 沒金鑰不會壞

沒設定或網路掛掉會**自動退回固定值，並在畫面標明**。
Demo Day 現場網路不穩不會讓簡報開天窗，而且退回時 `source` 欄位會寫
「[假資料] 模型呼叫失敗」——不會假裝成真的。

---

## 9. 實際跑之後抓到的四個 bug

**這一節的重點是：不實際跑就不會發現。**

### ① JSON 被 markdown 圍籬包住

即使指定了 `response_format=json_object`，模型還是回傳 ` ```json {...} ``` `，
`json.loads` 直接掛掉。

退路機制有接住沒讓 demo 崩潰——**但第二個測試因此顯示「可自動進行」，
跟它要示範的「轉人工」完全相反**。沒實際跑的話，這個會一路帶進錄影。

修法：先剝圍籬、失敗再抓第一個 `{` 到最後一個 `}`。四種格式都測過。

### ② `VleiVerifier` 的判定邏輯是錯的

原本比對整段輸出裡有沒有 `"VALID"`。但 sandbox 每次都會印
`LEI 8945002...: valid (ISO 17442-1)`——**那只代表檢查碼合格，不代表整條鏈通過**。

用這個判斷會把已撤銷的憑證判成有效，也就是「離職員工照樣放行」，
**剛好是這個 demo 要證明的反面**。

修法：判讀 `RESULT:` 行加退出碼，撤銷則看標記 `[FAIL]` 的那一行。

### ③ 預設事故敘述缺年份

越南文原句只說「ngày 26 tháng 8」，沒有年份。模型正確地回 null、
規則正確地轉人工——但這讓順利路徑看起來像失敗。敘述補上年份。

### ④ 中日韓字元的框線寬度

終端輸出的框線用 `len()` 算寬度，把雙寬字元算成一格，框線全歪。
改用 `unicodedata.east_asian_width` 判斷。

---

## 10. 目前狀態與未完成項目

### 真的在跑

- **vLEI 憑證驗證** — vlei-sandbox 實際簽發 GLEIF → QVI → 仲介 LE → 承辦人 ECR，
  林志豪的 ECR 已用 `revoke` 撤銷
- **母語理解** — `claude-sonnet-5` 實際抽取，轉人工用欄位完整性判斷
- **政策閘所有判斷** — 撤銷、到期、範圍、風險
- **稽核鏈雜湊與竄改偵測**

### 還是固定值

- 身份與聘僱關係查核（要接勞動部或人資系統，不是兩天能做的，放 Roadmap）
- 保障比對、缺件清單、案件進度、送件結果

### 未完成

| 項目 | 說明 |
|---|---|
| **SAID 寫死** | 每次重建 sandbox 都會產生新 SAID，程式碼裡是寫死的。多人協作會對不上。**正確做法是自動從 sandbox 讀取，還沒改** |
| 法條出處 | `match_coverage()` 的 `citation_note` 刻意留空，待查證原文後補 |
| 簡報 LEI | 情境說明頁面上還是舊的無效 LEI（結尾 01），要改 |
| 誠實限制頁 | 簡報還沒有這一節。程式裡標得很完整，但評審看的是簡報 |

### 交件狀態

| 項目 | 狀態 |
|---|---|
| 程式碼儲存庫 | ✅ https://github.com/wusrichard/migrant-insurance-infrastructure |
| README | ✅ |
| 第三方套件授權揭露 | ✅ |
| 合成資料聲明 | ✅ FIXTURES.md + 畫面標注 |
| 簡報 + 治理頁 | ⬜ |
| Demo 錄影 | ⬜ |
